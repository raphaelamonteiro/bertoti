# 🪄Strategy Pattern

O Strategy Pattern permite que o algoritmo varie independentemente de quem o utilizará. 
Se precisarmos adicionar mais uma classe não precisaremos mais criar ifs, apenas uma classe para esse novo meio, deixando o código mais limpo e com menos acoplamento.

- Strategy: é uma interface onde será definido um “contrato” para todos.

